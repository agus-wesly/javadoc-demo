# JAVADOC DEMO PROGRAM

## How to run

```
javac src/*.java -d output
```

```
cd output
java Main.java
```

## Docs

The docs file is inside the `docs` directory. To run it simply run the http server

```
cd docs
python -m http.server 6969
```

Open this link in your browser : http://localhost:6969
